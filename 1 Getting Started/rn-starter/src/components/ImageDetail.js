import React from "react";
import { view, Text, StyleSheet } from "react-native";

const ImageDetail = (props) => {
  return <Text>This is image detail {props.title}.</Text>;
};

const styles = StyleSheet.create({});

export default ImageDetail;
